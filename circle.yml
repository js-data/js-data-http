general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 5.7.0
dependencies:
  pre:
    - npm i -g npm
    - npm i -g codecov
    - npm i js-data@beta
test:
  post:
    - cat ./coverage/lcov.info | codecov
general:
  artifacts:
    - "dist"
    - "fetch/dist"
